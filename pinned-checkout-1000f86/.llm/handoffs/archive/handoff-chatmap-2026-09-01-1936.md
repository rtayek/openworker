# Download control result

The chat surface did not convert the local workspace link into a download button. The response can create the file, but it cannot force the user interface to display an attachment control.
